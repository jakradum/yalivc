import { SectionDivider } from '@/decks/design-system/blocks/SectionDivider/SectionDivider';

export function SectionDividerSlide({ heading, sub }) {
  return <SectionDivider heading={heading} sub={sub} />;
}
