import { SectionDivider } from '@/decks/design-system/blocks/SectionDivider/SectionDivider';

export function SectionDividerSlide({ heading, sub, pattern }) {
  return <SectionDivider heading={heading} sub={sub} pattern={pattern} />;
}
